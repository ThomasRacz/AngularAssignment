import { ContentFilterPipePipe } from './content-filter-pipe.pipe';

describe('ContentFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
